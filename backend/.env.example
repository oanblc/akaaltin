# Server
PORT=5001
NODE_ENV=development

# Database (MySQL)
DATABASE_URL="mysql://root:password@localhost:3306/aka_kuyumculuk"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Ana Fiyat Kaynağı (VPS WebSocket)
VPS_WS_URL=wss://your-vps-websocket-url

# Yedek Fiyat Kaynağı
FALLBACK_API_URL=https://saglamoglualtin.com/component/tab-group/1

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Fallback Ayarları
FALLBACK_TIMEOUT_SECONDS=60
AUTO_FALLBACK_ENABLED=true
